# BMI Tracker Application

แอปพลิเคชันติดตามและจัดการค่า BMI (Body Mass Index) ของคุณด้วยอินเตอร์เฟซกราฟิก (GUI) ใช้งานง่าย สามารถเก็บประวัติ, ดูกราฟ, และได้รับคำแนะนำด้านสุขภาพ

---

## 📋 สารบัญ
- [ความต้องการของระบบ](#ความต้องการของระบบ)
- [วิธีการติดตั้ง](#วิธีการติดตั้ง)
- [วิธีรันแอปพลิเคชัน](#วิธีรันแอปพลิเคชัน)
- [คุณสมบัติหลัก](#คุณสมบัติหลัก)
- [โครงสร้างโค้ด](#โครงสร้างโค้ด)
- [ไฟล์ที่สำคัญ](#ไฟล์ที่สำคัญ)
- [คำถามที่พบบ่อย](#คำถามที่พบบ่อย)

---

## ความต้องการของระบบ

### ตัวเลือก 1: รันไฟล์ exe (แนะนำ - ไม่ต้องติดตั้ง Java)
- **Windows 7 / 10 / 11**
- **หน่วยความจำ:** 200 MB ขึ้นไป
- **พื้นที่ว่าง:** 300 MB

### ตัวเลือก 2: รันจาก JAR (ต้องติดตั้ง Java)
- **Java Runtime Environment (JRE) 11** หรือสูงกว่า
- **ดาวน์โหลด:** https://www.oracle.com/java/technologies/downloads/

### ตัวเลือก 3: คอมไพล์และรันจากซอร์สโค้ด (สำหรับนักพัฒนา)
- **Java Development Kit (JDK) 11** หรือสูงกว่า
- **Compiler:** javac (รวมกับ JDK)

---

## วิธีการติดตั้ง

### ตัวเลือก 1: ใช้ไฟล์ Executable (exe) - ง่ายที่สุด

ไฟล์ exe มีทุกสิ่งที่ต้องการแล้ว (รวมถึง Java Runtime) ไม่ต้องทำอะไรพิเศษ

```
📁 installer/
└── 📁 BMITrackerApp/
    ├── 📄 BMITrackerApp.exe      ← ดับเบิลคลิกเพื่อเปิด
    ├── 📁 app/                   (ไฟล์แอปพลิเคชัน)
    └── 📁 runtime/               (Java Runtime บรรจุอยู่)
```

**ขั้นตอน:**
1. นำโฟลเดอร์ `installer/BMITrackerApp` ไปวางบนเครื่องของคุณหรือ USB
2. ดับเบิลคลิก `BMITrackerApp.exe` → แอปจะเปิดขึ้นทันที

### ตัวเลือก 2: ใช้ไฟล์ JAR

**ความต้องการ:** Java 11+ ติดตั้งบนเครื่อง

**ขั้นตอน:**
1. ติดตั้ง Java (JRE หรือ JDK) จาก https://www.oracle.com/java/technologies/downloads/
2. นำไฟล์ `BMICalculatorApp.jar` (อยู่ในโฟลเดอร์ `src`)
3. เปิด Command Prompt หรือ PowerShell แล้วรัน:
   ```powershell
   java -jar BMICalculatorApp.jar
   ```

### ตัวเลือก 3: คอมไพล์จากซอร์สโค้ด (สำหรับนักพัฒนา)

**ความต้องการ:** JDK 11+ ติดตั้ง

**ขั้นตอน:**
1. เปิด Command Prompt/PowerShell ในโฟลเดอร์ `src`
2. คอมไพล์ไฟล์ Java ทั้งหมด:
   ```powershell
   javac *.java
   ```
3. สร้าง JAR runnable:
   ```powershell
   jar cfm BMICalculatorApp.jar manifest.txt *.class
   ```
4. รัน:
   ```powershell
   java -jar BMICalculatorApp.jar
   ```

---

## วิธีรันแอปพลิเคชัน

### วิธีที่ 1: ดับเบิลคลิก exe (เร็วที่สุด)
```
💻 ดับเบิลคลิก: installer/BMITrackerApp/BMITrackerApp.exe
→ แอปเปิดขึ้นทันที ✓
```

### วิธีที่ 2: รันจาก PowerShell/CMD
```powershell
# หากมี Java ติดตั้ง
java -jar src\BMICalculatorApp.jar

# หรือใช้ exe
.\installer\BMITrackerApp\BMITrackerApp.exe
```

### วิธีที่ 3: สร้าง Shortcut บนหน้าจอ (Windows)
1. คลิกขวาที่ `BMITrackerApp.exe` → "Create shortcut"
2. ลากโครงร่างไปที่หน้าจอ
3. ดับเบิลคลิก shortcut เพื่อเปิดแอป

---

## คุณสมบัติหลัก

### 🏠 หน้าต้อนรับ (Welcome)
- ข้อมูลเกี่ยวกับแอป
- ปุ่มเริ่มการติดตามและดูประวัติ

### 📝 หน้ากรอกข้อมูล (Enter Details)
- กรอกน้ำหนัก (กิโลกรัม)
- กรอกส่วนสูง (เซนติเมตร)
- ตรวจสอบข้อมูล (validation)

### 📊 หน้าผลลัพธ์ (BMI Results)
- แสดงค่า BMI ที่คำนวณได้
- แสดงสถานะน้ำหนัก (Underweight, Normal, Overweight, Obese)
- แสดงการเปลี่ยนแปลงจากครั้งก่อน
- คำแนะนำด้านสุขภาพตามสถานะ
- ปุ่ม Share (คัดลอกไปคลิปบอร์ด) และ Save Record

### 📈 หน้ากราฟ (BMI Chart)
- แสดงกราฟแนวโน้ม BMI ตามเวลา
- บันทึกวันที่ของแต่ละการวัด
- ติดตามความคืบหน้าของคุณ

### 📋 หน้าประวัติ (History)
- รายการบันทึก BMI ทั้งหมด
- ดูรายละเอียด: วันที่, ค่า BMI, สถานะน้ำหนัก
- แก้ไขข้อมูล (Edit)
- ลบบันทึก (Delete)
- เลือกบันทึก → กด Edit หรือ Delete

---

## โครงสร้างโค้ด

แอปประกอบด้วยคลาสหลัก 6 คลาส ที่ดำเนินการต่าง ๆ ดังนี้:

### 1. **Main.java** - จุดเข้าของแอปพลิเคชัน
**หน้าที่:** สร้างและเริ่มต้น GUI
```java
public class Main {
    public static void main(String[] args) {
        new BMITrackerGUI();  // สร้าง GUI window
    }
}
```

### 2. **BMITrackerGUI.java** - อินเตอร์เฟซผู้ใช้ (GUI)
**ขนาด:** ~750 บรรทัด  
**หน้าที่:**
- จัดการ UI ทั้ง 5 หน้า (Welcome, Input, Results, Chart, History)
- ดำเนินการกับปุ่มและอินพุตของผู้ใช้
- อัปเดตแสดงผลและกราฟ

**เมธอดสำคัญ:**
- `initializeUI()` - สร้าง tabbed pane และหน้าทั้งหมด
- `createWelcomePage()` - สร้างหน้าต้อนรับ
- `createInputPage()` - สร้างหน้ากรอกข้อมูล
- `createResultsPage()` - สร้างหน้าผลลัพธ์
- `createChartPage()` - สร้างหน้ากราฟ
- `createHistoryPage()` - สร้างหน้าประวัติ
- `calculateBMI()` - เรียกฟังก์ชันคำนวณ BMI
- `saveRecord()` - บันทึกลงฐานข้อมูล
- `drawChart()` - วาดกราฟแนวโน้ม

### 3. **BMICalculator.java** - ตรรมชาติและการคำนวณ
**หน้าที่:** คำนวณค่า BMI และจำแนกประเภท
```java
static double calculateBMI(double weight, double height)
    // สูตร: BMI = weight / (height/100)²
    // ตัวอย่าง: น้ำหนัก 70 กก., ส่วนสูง 170 ซม. → BMI = 24.2

static String categorizeBMI(double bmi)
    // Underweight: BMI < 18.5
    // Normal: 18.5 ≤ BMI < 25
    // Overweight: 25 ≤ BMI < 30
    // Obese: BMI ≥ 30

static String getHealthRecommendation(String category)
    // แสดงคำแนะนำสุขภาพตามประเภท
```

### 4. **BMIDataManager.java** - การจัดการข้อมูลและไฟล์
**หน้าที่:** บันทึก อ่าน ลบ แก้ไข บันทึก BMI

**เมธอดสำคัญ:**
- `addRecord(BMIRecord)` - บันทึกข้อมูลใหม่ + เก็บไฟล์
- `getAllRecords()` - ดึงบันทึกทั้งหมด
- `deleteRecord(index)` - ลบบันทึกตามลำดับที่
- `loadRecords()` - โหลดจากไฟล์ (ที่เปิดแอปแรก)
- `saveRecords()` - บันทึกลงไฟล์ `bmi_data.dat` (Serialization)

**ที่เก็บข้อมูล:** `bmi_data.dat` (ไฟล์ binary ใช้ Java Serialization)

### 5. **BMIRecord.java** - โมเดลข้อมูล
**หน้าที่:** เก็บข้อมูลหนึ่งครั้งการวัด
```java
public class BMIRecord implements Serializable {
    double weight;           // น้ำหนัก (กก.)
    double height;           // ส่วนสูง (ซม.)
    double bmi;              // ค่า BMI ที่คำนวณ
    String category;         // ประเภท (Normal, Obese, ...)
    Date recordDate;         // วันที่วัด
    // ...
    String getFormattedDate()  // แสดงวันที่แบบอ่านง่าย
}
```

### 6. **InputValidator.java** - ตรวจสอบความถูกต้องของอินพุต
**หน้าที่:** ตรวจค่าน้ำหนักและส่วนสูงก่อนใช้
```java
static String validateInput(double weight, double height)
    // ตรวจ:
    // - น้ำหนัก: 1 - 500 กก.
    // - ส่วนสูง: 50 - 300 ซม.
    // - คืนค่า: null (ถ้าถูก) หรือข้อความ error (ถ้าผิด)
```

---

## ไฟล์ที่สำคัญ

```
📦 04_Implementation/
├── 📁 src/                          ← โค้ดต้นฉบับและไฟล์สำคัญ
│   ├── 📄 Main.java                 ← จุดเข้าของแอป
│   ├── 📄 BMITrackerGUI.java        ← อินเตอร์เฟซ UI (5 หน้า)
│   ├── 📄 BMICalculator.java        ← คำนวณ BMI และจำแนกประเภท
│   ├── 📄 BMIDataManager.java       ← จัดการบันทึก
│   ├── 📄 BMIRecord.java            ← โมเดลข้อมูล
│   ├── 📄 InputValidator.java       ← ตรวจสอบอินพุต
│   ├── 📄 manifest.txt              ← ระบุคลาสหลัก (Main-Class)
│   ├── 📄 BMICalculatorApp.jar      ← JAR runnable (รวมทั้งหมด)
│   ├── 📄 bmi_data.dat              ← ไฟล์บันทึก BMI (Serialized)
│   │                                  ⚠️ สร้างตัวเองเมื่อรันครั้งแรก
│   └── 📁 installer/                ← แอปสมบูรณ์ (พร้อมรันได้)
│       └── 📁 BMITrackerApp/
│           ├── 📄 BMITrackerApp.exe ← ไฟล์หลัก (ดับเบิลคลิกเพื่อเปิด)
│           ├── 📁 app/              ← ไฟล์แอปพลิเคชัน
│           └── 📁 runtime/          ← Java Runtime (บรรจุอยู่)
└── 📄 README.md                     ← ไฟล์นี้
```

### คำอธิบายไฟล์สำคัญ
| ไฟล์ | ประเภท | คำอธิบาย |
|------|--------|----------|
| `Main.java` | Source | จุดเข้าของแอป - โปรแกรมเริ่มจากที่นี่ |
| `BMITrackerGUI.java` | Source | UI ทั้งหมด (ใหญ่ที่สุด ~750 บรรทัด) |
| `BMICalculator.java` | Source | ฟังก์ชันคำนวณและจำแนก BMI |
| `BMIDataManager.java` | Source | บันทึก อ่าน ลบ แก้ไขข้อมูล |
| `BMIRecord.java` | Source | โครงสร้างข้อมูลหนึ่งบันทึก |
| `InputValidator.java` | Source | ตรวจสอบความถูกต้องของอินพุต |
| `BMICalculatorApp.jar` | Compiled | JAR runnable (รวม .class ทั้งหมด) |
| `bmi_data.dat` | Data | ไฟล์บันทึก (Java Serialized Object) |
| `manifest.txt` | Config | ระบุคลาสหลักของ JAR |
| `BMITrackerApp.exe` | Executable | ไฟล์ exe (รวม runtime) - รันได้ทันที |

---

## คำถามที่พบบ่อย

### Q: ต้องติดตั้ง Java หรือไม่?
**A:** 
- ถ้าใช้ `BMITrackerApp.exe` ✅ ไม่ต้อง (รวม Java Runtime อยู่)
- ถ้าใช้ `BMICalculatorApp.jar` ❌ ต้อง (Java 11+)
- ถ้าคอมไพล์เอง ❌ ต้อง JDK

### Q: บันทึก BMI เก็บไว้ที่ไหน?
**A:** ไฟล์ `bmi_data.dat` ในโฟลเดอร์ `src` (ที่รัน exe/jar)  
เป็นไฟล์ binary ใช้ Java Serialization

### Q: แก้ไขบันทึกเก่าได้หรือไม่?
**A:** ใช่ → เปิดแท็บ "History" → คลิกบันทึก → กด "Edit Entry" → เปลี่ยนค่า → Save

### Q: ลบบันทึกได้หรือไม่?
**A:** ใช่ → เปิดแท็บ "History" → คลิกบันทึก → กด "Delete Entry" → ยืนยัน

### Q: สร้าง Backup ข้อมูลได้หรือไม่?
**A:** ใช่ → คัดลอก `bmi_data.dat` ไปอื่น (วางไว้ใน USB หรือ Cloud)  
เวลาต้องใช้ → นำมาวางกลับในโฟลเดอร์

### Q: รันได้บนเครื่องอื่น Mac/Linux หรือไม่?
**A:**
- `BMITrackerApp.exe` → Windows เท่านั้น
- `BMICalculatorApp.jar` → Windows, Mac, Linux (มี Java 11+)

### Q: ทำไมเปิดแล้วไม่ขึ้น?
**A:** 
- ✅ ตรวจ: `bmi_data.dat` อยู่ในโฟลเดอร์เดียวกับ exe/jar หรือไม่
- ✅ ลบ `bmi_data.dat` และรันใหม่ (แอปจะสร้างไฟล์ใหม่)
- ✅ ตรวจ Java version: `java -version`
- ✅ รัน PowerShell เป็น Admin

### Q: ต้องทำอะไรถ้ากรอกข้อมูลผิด?
**A:** 
- แท็บ "Enter Details" → กด Reset → กรอกใหม่ → Calculate

### Q: วิธีลบทั้งหมดและเริ่มใหม่?
**A:** 
- ลบไฟล์ `bmi_data.dat` 
- รันแอปใหม่ → จะสร้างไฟล์ใหม่แบบว่าง

---

## 📞 การติดต่อและสนับสนุน

หากพบปัญหา:
1. ตรวจสอบ **คำถามที่พบบ่อย** ด้านบน
2. ลองลบ `bmi_data.dat` และรันใหม่
3. ตรวจสอบเวอร์ชัน Java: `java -version`
4. รัน PowerShell/CMD เป็น Administrator

---

**เวอร์ชัน:** 1.0  
**อัปเดตครั้งสุดท้าย:** 30 พฤศจิกายน 2568  
**สร้างด้วย:** Java Swing (GUI)

---

**ขอให้มีความสุขในการติดตามสุขภาพของคุณ! 💪📊**

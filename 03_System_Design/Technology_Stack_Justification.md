ภาษาโปรแกรม: Java  
สาเหตุที่เลือก : ภาษาที่เสถียร, ปลอดภัย, และเป็นมาตรฐาน พกพาได้ เหมาะสำหรับระบบ health tracking ที่ต้องความเชื่อถือได้ Swing

Framework: ไม่มี  
สาเหตุที่เลือก : ลดความซับซ้อน ไม่ต้องติดตั้ง dependencies ตัวอักษรรายไฟล์ jar เดียว \- ส่งง่ายไม่ต้องอัปเดต framework ภายนอก เพียงพอสำหรับแอปพลิเคชันขนาดเล็ก-กลาง File-based Storage (Serialization)

Database : Java Object Serialization  
สาเหตุที่เลือก : ไม่ต้องติดตั้ง database server ตั้งค่าง่าย \- ไม่ต้องการ connection string, user/password เหมาะสำหรับแอปพลิเคชันส่วนตัว/เดสก์ทอป ข้อมูลเก็บไว้ในเครื่องเพื่อความเป็นส่วนตัว  
